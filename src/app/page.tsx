
'use client';

import React from 'react';
import RedirectPage from '@/app/(app)/page.tsx';

export default function LoginPage() {
  // Directly render the redirect logic.
  return <RedirectPage />;
}
